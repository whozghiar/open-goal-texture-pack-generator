[package]
name = "open-goal-texture-pack-generator"
version = "1.0.0"
description = "OpenGOAL Texture Pack Generator (Tauri v2 + Rust)"
authors = ["whozghiar"]
edition = "2021"

[lib]
name = "texture_pack_generator_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "2.2", default-features = false, features = ["deflate"] }
sha2 = "0.10"
walkdir = "2"
base64 = "0.22"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
